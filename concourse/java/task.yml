platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: alpine

inputs:
  - name: source
  - name: ExamPGR301-App

outputs:
  - name: jar-file

run:
  path: ExamPGR301-App/concourse/java/task.sh
